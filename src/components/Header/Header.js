import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <nav>
            <h2 className='head'>My flowers Store</h2>
        </nav>
    );
};

export default Header;